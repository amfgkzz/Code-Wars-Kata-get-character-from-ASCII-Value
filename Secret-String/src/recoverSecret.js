module.exports = 
function recoverSecret(triplet){
    let secretWord = '';
    let secretValue = {};

    const compare = function(a){

    }

    for (let i = 1; i < triplet.length; i++) {
        const compareArray = triplet[0];
        const element = triplet[i];
        
        element.map(compare);
    }
}