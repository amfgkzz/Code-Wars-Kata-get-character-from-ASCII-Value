module.exports = function maskify(string){
    let newString = "";

    for (let i = 0; i < string.length; i++) {
        const element = string[i];
        console.log(element);
    }

    return newString;
}