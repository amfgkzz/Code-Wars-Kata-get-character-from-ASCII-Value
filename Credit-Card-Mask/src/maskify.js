module.exports = function maskify(string){
    if ( string.length > 4 ) {
        let newString = "";
        for (let i = 0; i < array.length; i++) {
            const element = array[i];
            
        }
        return newString;
    } else {
        return string;
    }
}