let isWholeNumber = require('./isWholeNumber');

module.exports = function isSquare(n){
    if ( n > 0 && typeof number ) {
        console.log('hit');

        
        return false;
    }

    return false;
}